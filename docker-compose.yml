services:
  s3mgr:
    build: .
    image: s3-file-manager
    container_name: s3mgr
    ports:
      - "80:80"
    volumes:
      - ./data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:80"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
