events {}

http {

  server {

    listen 80;
    client_max_body_size 700M;

    location / {

      proxy_pass http://s3-file-manager:8000;

      proxy_set_header Host $host;
      proxy_set_header X-Real-IP $remote_addr;

    }

  }

}
